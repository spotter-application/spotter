import { Matrix } from '.';
/** return Matrix but filled with random numbers from gaussian
 */
export declare class RandomMatrix extends Matrix {
    std: number;
    constructor(rows: number, columns: number, std: number);
}
//# sourceMappingURL=random-matrix.d.ts.map